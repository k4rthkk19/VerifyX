# ============================================================
# VerifyX — Fake News Detection App
# Requirements
# ============================================================

# --- Web framework ---
flask>=3.0.0
flask-cors>=4.0.0

# --- AI / ML core ---
transformers>=4.38.0          # HuggingFace Transformers (DistilBERT)
torch>=2.1.0                  # PyTorch CPU backend
scikit-learn>=1.4.0           # Sklearn TF-IDF + LogReg fallback
numpy>=1.26.0

# --- NLP utilities ---
nltk>=3.8.1
# spacy>=3.7.0  # optional: uncomment if you want spaCy

# --- Web scraping (for URL analysis) ---
requests>=2.31.0
beautifulsoup4>=4.12.0
newspaper3k>=0.2.8            # best-effort article extraction (optional)
lxml>=5.0.0                   # fast HTML parser for BS4

# --- Database ---
# sqlite3 is part of the standard library — no extra package needed

# --- Production server ---
gunicorn>=21.2.0              # WSGI server for deployment

# --- Dev / testing ---
pytest>=8.0.0
